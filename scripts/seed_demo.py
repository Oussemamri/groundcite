"""Seed a local demo: ingest the fetched corpus and load the eval suites (spec §14).

Depends on the ingestion pipeline (spec §6) and the eval harness (spec §8), so it
is implemented once those exist (Weeks 1–3). Not implemented in the P5 skeleton.
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError("seed_demo is implemented once ingestion + evals exist (spec §6, §8).")


if __name__ == "__main__":
    main()
